<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".presentation.transfer.SelectPayeeActivity">

    <ListView
        android:id="@+id/lv_selectPayee"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/Style_ListView"
        android:layout_above="@+id/btn_selectPayee"/>

    <Button
        android:id="@+id/btn_selectPayee"
        style="@style/Style_Button"
        android:layout_centerInParent="true"
        android:layout_alignParentBottom="true"
        android:text="Select Payee" />

</RelativeLayout>